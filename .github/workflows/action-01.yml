# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a set of commands using the runners shell
      - name: Tiers Server Host
        env:
          TIERS_SERVER_HOST: ${{ secrets.TS_HOSTNAME }}
          TIERS_SERVER_UNAME: ${{ secrets.TS_USERNAME }}
          TIERS_SERVER_PASSWORD: ${{ secrets.TS_PASSWORD }}
        run: echo $TIERS_SERVER_HOST

      # - name: Tiers Server Username
      #   run: echo ${{ secrets.TIERS_SERVER_UNAME }}
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.TIERS_SERVER_HOST }}
      #     username: ${{ secrets.TIERS_SERVER_UNAME }}
      #     password: ${{ secrets.TIERS_SERVER_PASSWORD }}
      #     script: cd admin-savci && git pull origin main

      # - name: SSH Connection main server for running build
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.MAIN_SERVER_HOST }}
      #     username: ${{ secrets.MAIN_SERVER_UNAME }}
      #     password: ${{ secrets.MAIN_SERVER_PASSWORD }}
      #     script: ./deploy-finer.sh
